# mac_dev_setup


To append on zrc profile

alias cdf='cd $(fd --type d | fzf)'

# Switch across namespaces
alias kn=kubens

# Swich across context
alias kx=kubectx

# Search files
alias f=fzf

# kubectl control
alias k=kubectl

